from setuptools import setup, find_packages

setup(
    name="link_pred",
    version="0.0.0",
    description="",
    author="Timothee RIO",
    packages=["link_pred"],
    package_dir={
        "link_pred": "link_pred"}
)